services:
  - type: web
    name: tiny-outings
    runtime: node
    plan: free
    buildCommand: npm install && npm rebuild better-sqlite3 && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
